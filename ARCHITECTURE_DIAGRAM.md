# FictionLab App - Architecture Diagrams

## Current Architecture (Before Implementation)

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Clients                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐              ┌──────────────────┐        │
│  │  Typing Mind     │              │  Claude Desktop  │        │
│  │  (Web Browser)   │              │  (Manual Setup)  │        │
│  │  Port: 8080      │              │  Not Integrated  │        │
│  └────────┬─────────┘              └──────────────────┘        │
│           │                                                     │
└───────────┼─────────────────────────────────────────────────────┘
            │
            │ HTTP/SSE
            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Docker Compose Services                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  NGINX (Typing Mind Static Files)                       │  │
│  │  Port: 8080                                              │  │
│  └──────────────────┬───────────────────────────────────────┘  │
│                     │                                           │
│                     │ Proxies to                                │
│                     ▼                                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  MCP Connector (@typingmind/mcp)                         │  │
│  │  Port: 50880                                             │  │
│  │  Auth: MCP_AUTH_TOKEN                                    │  │
│  └──────────────────┬───────────────────────────────────────┘  │
│                     │                                           │
│                     │ HTTP Requests                             │
│                     ▼                                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  MCP Writing Servers (Bind Mount - SLOW on Windows)     │  │
│  │  ┌──────────┬──────────┬──────────┬─────────┬─────────┐ │  │
│  │  │ Book     │ Series   │ Chapter  │Character│  Scene  │ │  │
│  │  │ :3001    │ :3002    │ :3003    │ :3004   │ :3005   │ │  │
│  │  └──────────┴──────────┴──────────┴─────────┴─────────┘ │  │
│  │  ┌──────────┬──────────┬──────────┬─────────┐           │  │
│  │  │CoreCont  │ Review   │Reporting │ Author  │           │  │
│  │  │ :3006    │ :3007    │ :3008    │ :3009   │           │  │
│  │  └──────────┴──────────┴──────────┴─────────┘           │  │
│  └──────────────────┬───────────────────────────────────────┘  │
│                     │                                           │
│                     │ Direct Connection (No Pooling)            │
│                     ▼                                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  PostgreSQL 16                                           │  │
│  │  Port: 5432                                              │  │
│  │  Basic Config: max_connections=200                      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Network: writing-network (bridge, basic settings)             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

Issues:
❌ Bind mounts are 10-100x slower on Windows
❌ No connection pooling (overhead on every request)
❌ No health checks (can't detect failures)
❌ Claude Desktop requires manual configuration
❌ PostgreSQL not optimized for performance
```

---

## Proposed Architecture (After Implementation)

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Clients                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐              ┌──────────────────┐        │
│  │  Typing Mind     │              │  Claude Desktop  │        │
│  │  (Web Browser)   │              │  (Native App)    │        │
│  │  Port: 8080      │              │  stdio Protocol  │        │
│  │  ✅ Auto-Config  │              │  ✅ Auto-Config  │        │
│  └────────┬─────────┘              └──────────┬───────┘        │
│           │                                    │                │
│           │ HTTP/SSE                           │ stdio/TCP      │
│           │                                    │ (1-5ms!)       │
└───────────┼────────────────────────────────────┼────────────────┘
            │                                    │
            ▼                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Docker Compose Services                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  NGINX (Typing Mind Static Files)                       │  │
│  │  Port: 8080                                              │  │
│  │  ✅ Health Check                                         │  │
│  └──────────────────┬───────────────────────────────────────┘  │
│                     │                                           │
│                     ▼                                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  MCP Connector (@typingmind/mcp)         ┌─────────────┐ │  │
│  │  Port: 50880                             │Claude Bridge│ │  │
│  │  Auth: MCP_AUTH_TOKEN                    │Port: 8888   │ │  │
│  │  ✅ Health Check (NEW)                   │stdio/socat  │ │  │
│  └──────────────────┬───────────────────────┴─────────────┘  │  │
│                     │                          │               │
│                     │ HTTP                     │ stdio         │
│                     ▼                          ▼               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  MCP Writing Servers (IMAGE-BASED - FAST! 🚀)           │  │
│  │  ✅ Multi-stage Dockerfile (Task 2.1)                   │  │
│  │  ✅ server.js Orchestrator (Task 2.2)                   │  │
│  │  ✅ Health Check                                         │  │
│  │  ┌──────────┬──────────┬──────────┬─────────┬─────────┐ │  │
│  │  │ Book     │ Series   │ Chapter  │Character│  Scene  │ │  │
│  │  │ :3001    │ :3002    │ :3003    │ :3004   │ :3005   │ │  │
│  │  └──────────┴──────────┴──────────┴─────────┴─────────┘ │  │
│  │  ┌──────────┬──────────┬──────────┬─────────┐           │  │
│  │  │CoreCont  │ Review   │Reporting │ Author  │           │  │
│  │  │ :3006    │ :3007    │ :3008    │ :3009   │           │  │
│  │  └──────────┴──────────┴──────────┴─────────┘           │  │
│  └──────────────────┬───────────────────────────────────────┘  │
│                     │                                           │
│                     │ Connection Pooling                        │
│                     ▼                                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  PgBouncer (NEW - Task 1.2)                              │  │
│  │  Port: 6432                                              │  │
│  │  Pool Mode: transaction                                 │  │
│  │  Max Connections: 200                                   │  │
│  │  Pool Size: 25                                          │  │
│  │  ✅ 30x Less Overhead! 🚀                               │  │
│  └──────────────────┬───────────────────────────────────────┘  │
│                     │                                           │
│                     │ Pooled Connections                        │
│                     ▼                                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  PostgreSQL 16 (OPTIMIZED - Task 1.1)                   │  │
│  │  Port: 5432                                              │  │
│  │  ✅ shared_buffers=256MB                                │  │
│  │  ✅ effective_cache_size=1GB                            │  │
│  │  ✅ work_mem=10MB                                       │  │
│  │  ✅ 20-30% Faster Queries! 🚀                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Network: writing-network (bridge, OPTIMIZED - Task 3.2)       │
│  ✅ MTU: 1500, Named bridge: br-writing                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

Benefits:
✅ 10-100x faster startup (image-based build)
✅ 30x less connection overhead (PgBouncer)
✅ 20-30% faster queries (PostgreSQL tuning)
✅ 1-5ms latency for Claude Desktop (stdio)
✅ Auto-configuration for both clients
✅ Health monitoring for all services
✅ Production-ready infrastructure
```

---

## Implementation Phases Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      PHASE 1: Database                          │
│                   (Low Risk, High Impact)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Step 1: Task 1.1 - PostgreSQL Tuning                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Add performance parameters to postgres command           │  │
│  │ • shared_buffers=256MB                                   │  │
│  │ • effective_cache_size=1GB                               │  │
│  │ • work_mem=10MB                                          │  │
│  │ • random_page_cost=1.1                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│           ↓                                                     │
│  Step 2: Task 1.2 - PgBouncer                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Add PgBouncer service between MCP servers and DB         │  │
│  │ • Connection pooling enabled                             │  │
│  │ • 30x reduction in overhead                              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ✅ TEST THOROUGHLY BEFORE PROCEEDING                           │
│                                                                 │
└───────────────────────────┬─────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                   PHASE 2: Reliability                          │
│                   (Low Risk, Quick Wins)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Step 3: Task 3.1 - Connector Health Check                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Add health check to MCP Connector                        │  │
│  │ • Automatic failure detection                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│           ↓                                                     │
│  Step 4: Task 3.2 - Network Optimization                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Optimize Docker network settings                         │  │
│  │ • Better stability and naming                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ✅ TEST THOROUGHLY BEFORE PROCEEDING                           │
│                                                                 │
└───────────────────────────┬─────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                  PHASE 3: Build Optimization                    │
│               (Medium Risk, Very High Impact)                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Step 5: Task 2.1 - Multi-stage Dockerfile                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Create optimized Dockerfile in mcp-writing-servers repo  │  │
│  │ • 3-stage build (deps, builder, runtime)                 │  │
│  │ • Non-root user for security                             │  │
│  └──────────────────────────────────────────────────────────┘  │
│           ↓                                                     │
│  Step 6: Task 2.2 - Server Orchestrator                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Create server.js in mcp-writing-servers repo             │  │
│  │ • Manages all 9 MCP servers                              │  │
│  │ • Graceful shutdown, unified logging                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│           ↓                                                     │
│  Step 7: Task 2.3 - Image-based Build                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Update docker-compose to use image instead of volume     │  │
│  │ • Remove bind mount (huge performance gain!)             │  │
│  │ • Add health check                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ✅ TEST THOROUGHLY BEFORE PROCEEDING                           │
│  ⚠️  Requires rebuilding Docker images (one-time)              │
│                                                                 │
└───────────────────────────┬─────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                 PHASE 4: Claude Desktop                         │
│                (High Complexity, High Impact)                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Step 8: Task 4.1 - stdio Adapter                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Create mcp-stdio-adapter.js (mcp-writing-servers repo)   │  │
│  │ • Bridges stdio ↔ HTTP                                   │  │
│  │ • JSON-RPC protocol handling                             │  │
│  └──────────────────────────────────────────────────────────┘  │
│           ↓                                                     │
│  Step 9: Task 4.2 - Bridge Service                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Add claude-bridge service to docker-compose              │  │
│  │ • socat for TCP-to-stdio bridging                        │  │
│  │ • Port 8888 for Claude Desktop connection                │  │
│  └──────────────────────────────────────────────────────────┘  │
│           ↓                                                     │
│  Step 10: Task 4.3 - Auto-config Module                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Create claude-desktop-auto-config.ts (Electron app)      │  │
│  │ • Generates claude_desktop_config.json                   │  │
│  │ • Platform-specific paths (Windows/Mac/Linux)            │  │
│  │ • All 9 MCP servers configured                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│           ↓                                                     │
│  Step 11: Task 4.4 - UI Integration                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Add Claude Desktop UI to Electron app                    │  │
│  │ • Auto-configure button                                  │  │
│  │ • Status display                                         │  │
│  │ • Config preview and management                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ✅ FINAL INTEGRATION TESTING                                   │
│  • Test both Typing Mind AND Claude Desktop                    │
│  • Verify all 9 MCP servers accessible from both clients       │
│  • Check performance metrics                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Dependency Tree

```
Tasks by Dependency Level

Level 0 (Independent - Can start immediately):
├── 1.1 PostgreSQL Tuning
├── 2.1 Dockerfile
├── 2.2 Orchestrator
├── 3.1 Health Check
├── 3.2 Network
└── 4.1 stdio Adapter

Level 1 (Depends on Level 0):
├── 1.2 PgBouncer ───────────────► Depends on 1.1
├── 2.3 Image Build ──────────────► Depends on 2.1, 2.2
└── 4.2 Bridge ───────────────────► Depends on 4.1

Level 2 (Depends on Level 1):
└── 4.3 Auto-config ──────────────► Depends on 4.1, 4.2

Level 3 (Depends on Level 2):
└── 4.4 UI ───────────────────────► Depends on 4.3
```

---

## File Structure Changes

```
MCP-Electron-App/
├── docker-compose.yml ──────────── Modified by Tasks 1.1, 1.2, 2.3, 3.1, 3.2, 4.2
├── src/
│   ├── main/
│   │   ├── index.ts ─────────────── Modified by Task 4.4 (IPC handlers)
│   │   └── claude-desktop-auto-config.ts ─ NEW (Task 4.3)
│   ├── preload/
│   │   └── preload.ts ───────────── Modified by Task 4.4 (API)
│   └── renderer/
│       ├── index.html ───────────── Modified by Task 4.4 (UI)
│       └── client-selection-handlers.ts ─ Modified by Task 4.4 (handlers)
├── scripts/
│   ├── create-github-issues.js ── NEW (Issue creator)
│   ├── create-issues.sh ────────── NEW (Issue creator bash)
│   └── README.md ───────────────── NEW (Script docs)
├── IMPLEMENTATION_TASKS.md ──────── NEW (Task details)
├── QUICK_START.md ───────────────── NEW (Getting started)
├── IMPLEMENTATION_SUMMARY.md ────── NEW (Overview)
└── ARCHITECTURE_DIAGRAM.md ──────── NEW (This file)

mcp-writing-servers/ (SEPARATE REPOSITORY)
├── Dockerfile ───────────────────── NEW (Task 2.1)
├── Dockerfile.claude-bridge ────── NEW (Task 4.2)
├── server.js ────────────────────── NEW (Task 2.2)
├── mcp-stdio-adapter.js ─────────── NEW (Task 4.1)
└── src/
    └── mcps/
        ├── book-planning-server/
        ├── series-planning-server/
        ├── chapter-planning-server/
        ├── character-planning-server/
        ├── scene-server/
        ├── core-continuity-server/
        ├── review-server/
        ├── reporting-server/
        └── author-server/
```

---

## Performance Comparison

### Before Implementation

```
┌────────────────────────────────────────────────────────────┐
│ Metric                    │ Current State                  │
├────────────────────────────────────────────────────────────┤
│ Container Startup Time    │ 30-60 seconds (Windows)        │
│ Database Query Time       │ 100ms (baseline)               │
│ Connection Overhead       │ High (new conn per request)    │
│ Claude Desktop Setup      │ Manual, complex                │
│ Typing Mind Setup         │ Auto (working)                 │
│ Health Monitoring         │ Limited (PostgreSQL only)      │
│ Network Performance       │ Basic bridge settings          │
└────────────────────────────────────────────────────────────┘
```

### After Implementation

```
┌────────────────────────────────────────────────────────────┐
│ Metric                    │ Target State                   │
├────────────────────────────────────────────────────────────┤
│ Container Startup Time    │ 0.3-6 seconds 🚀 (10-100x)    │
│ Database Query Time       │ 70-80ms 🚀 (20-30% faster)    │
│ Connection Overhead       │ Very Low 🚀 (30x reduction)   │
│ Claude Desktop Setup      │ Auto, one-click ✅             │
│ Typing Mind Setup         │ Auto (unchanged) ✅            │
│ Health Monitoring         │ All services ✅                │
│ Network Performance       │ Optimized settings ✅          │
└────────────────────────────────────────────────────────────┘
```

---

## Data Flow: Typing Mind Request

### Before

```
User types in browser
    ↓
NGINX serves static page
    ↓
JavaScript calls MCP Connector API (localhost:50880)
    ↓
MCP Connector forwards to MCP Server (localhost:3001)
    ↓
MCP Server connects to PostgreSQL (localhost:5432)
    ↓
PostgreSQL executes query
    ↓
Response back through chain

Total Latency: ~50-100ms
```

### After

```
User types in browser
    ↓
NGINX serves static page
    ↓
JavaScript calls MCP Connector API (localhost:50880)
    ↓ ✅ Health Check
MCP Connector forwards to MCP Server (localhost:3001)
    ↓
MCP Server uses PgBouncer pool (localhost:6432)
    ↓ ✅ Connection Pool (30x faster)
PgBouncer forwards to PostgreSQL (localhost:5432)
    ↓ ✅ Optimized Settings
PostgreSQL executes query faster
    ↓
Response back through chain

Total Latency: ~35-70ms (30% improvement)
```

---

## Data Flow: Claude Desktop Request

### Before (Manual Setup)

```
❌ User must manually:
   1. Find Claude Desktop config file
   2. Edit JSON by hand
   3. Find MCP server paths
   4. Configure each of 9 servers
   5. Restart Claude Desktop

❌ No direct connection to Dockerized servers
❌ Requires complex stdio setup
❌ Error-prone
```

### After (Automated)

```
User clicks "Auto-Configure" button in Electron app
    ↓
Electron app generates claude_desktop_config.json
    ↓ ✅ Platform-specific path
    ↓ ✅ All 9 servers configured
    ↓ ✅ stdio bridge configured
Writes config to correct location
    ↓
User restarts Claude Desktop
    ↓
Claude Desktop connects via stdio (port 8888)
    ↓ ✅ Ultra-low latency (1-5ms)
stdio bridge (socat) forwards to mcp-stdio-adapter
    ↓
Adapter converts stdio → HTTP
    ↓
MCP Server processes request
    ↓
Uses PgBouncer pool → PostgreSQL
    ↓
Response back through chain (HTTP → stdio)

Total Latency: ~5-15ms (10x faster than HTTP!)
```

---

## Summary

This implementation transforms your FictionLab App from a development prototype into a production-ready application with:

✅ **Performance**: 10-100x faster startup, 20-30% faster queries
✅ **Scalability**: Connection pooling handles concurrent requests
✅ **Reliability**: Health checks on all services
✅ **User Experience**: One-click setup for both clients
✅ **Architecture**: Production-ready Docker infrastructure
✅ **Foundation**: Ready for future clients (Obsidian, custom clients)

Perfect for your target users: **non-technical genre fiction authors** who need powerful MCP tools without the complexity!

---

**Created**: 2025-11-15
**Last Updated**: 2025-11-15
**Version**: 1.0
