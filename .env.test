# Test environment configuration for MCP-Electron-App
# This is for local testing only

# MCP Writing Servers Repository
MCP_WRITING_SERVERS_DIR=C:/github/MCP-Writing-Servers

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=testpassword123
POSTGRES_DB=writing_db

# TypingMind Configuration
TYPING_MIND_DIR=C:/github/MCP-Electron-App/typingmind
NGINX_CONF_PATH=C:/github/MCP-Electron-App/resources/templates/nginx.conf

# MCP Connector
MCP_AUTH_TOKEN=test-token-12345

# Node Environment
NODE_ENV=development
